name: Compilar Plugin

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    # Se especifica el directorio de trabajo donde está tu código
    defaults:
      run:
        working-directory: .

    steps:
    - uses: actions/checkout@v4

    - name: Configurar Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Compilar con Maven
      # Cambia "TpuPerks" por el nombre exacto de la subcarpeta si tu pom.xml está dentro de una
      run: mvn clean package --batch-mode
      working-directory: ./TpuPerks

    - name: Guardar JAR generado
      uses: actions/upload-artifact@v4
      with:
        name: TpuPerks-Plugin
        path: '**/target/*.jar'
